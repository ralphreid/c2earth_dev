= form_for @investigation_type do |f|
  - if @investigation_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@investigation_type.errors.count, "error")} prohibited this investigation_type from being saved:"
      %ul
        - @investigation_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :investigation_type
    = f.text_field :investigation_type
  .actions
    = f.submit 'Save'
